<Window x:Class="WPFBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFBrowser"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox x:Name="_cb" Grid.Row="0" Grid.Column="0" Margin="4" IsEditable="True" Text="http://qwant.fr/" SelectionChanged="ComboBox_SelectionChanged"/>

        <Button Grid.Row="0" Grid.Column="1" Margin="4" Content="Go" Padding="16,4,16,4" Click="Button_Connect"/>

        <WebBrowser x:Name="_wb" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="4" Navigating="WebBrowser_Navigating"/>

    </Grid>
</Window>
